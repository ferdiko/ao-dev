{
  "name": "sovara-ui",
  "version": "1.0.0",
  "private": true,
  "description": "UI workspace for the Sovara VS Code extension and web app",
  "workspaces": [
    "shared_components",
    "vscode_extension",
    "web_app"
  ],
  "scripts": {
    "install:all": "npm --prefix vscode_extension install && npm --prefix web_app install",
    "build:all": "npm run build:extension && npm run build:webapp",
    "build:extension": "npm --prefix vscode_extension run compile",
    "build:webapp": "npm --prefix web_app run build",
    "dev:webapp": "npm --prefix web_app run dev",
    "lint:webapp": "npm --prefix web_app run lint",
    "test:webapp": "npm --prefix web_app run test",
    "clean": "rm -rf node_modules vscode_extension/node_modules web_app/node_modules"
  },
  "dependencies": {
  }
}
