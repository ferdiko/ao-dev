[project]
name = "sovara-examples-openai"
version = "0.1.0"
description = "Sovara debug examples for OpenAI"
requires-python = ">=3.10"
dependencies = [
    "sovara",
    "openai",
    "openai-agents",
    "e2b-code-interpreter",
]

[tool.uv.sources]
sovara = { path = "../../..", editable = true }
