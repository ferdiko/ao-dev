[project]
name = "sovara-examples-ollama"
version = "0.1.0"
description = "Sovara debug examples for Ollama"
requires-python = ">=3.10"
dependencies = [
    "sovara",
    "ollama",
]

[tool.uv.sources]
sovara = { path = "../../..", editable = true }
