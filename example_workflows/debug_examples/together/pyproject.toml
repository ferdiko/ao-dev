[project]
name = "sovara-examples-together"
version = "0.1.0"
description = "Sovara debug examples for Together AI"
requires-python = ">=3.10"
dependencies = [
    "sovara",
    "together",
]

[tool.uv.sources]
sovara = { path = "../../..", editable = true }
