[project]
name = "sovara-examples-mcp"
version = "0.1.0"
description = "Sovara debug examples for Model Context Protocol"
requires-python = ">=3.10"
dependencies = [
    "sovara",
    "mcp",
]

[tool.uv.sources]
sovara = { path = "../../..", editable = true }
