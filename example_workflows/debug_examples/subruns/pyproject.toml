[project]
name = "sovara-examples-subruns"
version = "0.1.0"
description = "Sovara debug examples for subruns/concurrent execution"
requires-python = ">=3.10"
dependencies = [
    "sovara",
    "openai",
]

[tool.uv.sources]
sovara = { path = "../../..", editable = true }
