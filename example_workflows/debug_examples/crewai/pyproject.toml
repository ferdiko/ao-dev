[project]
name = "sovara-examples-crewai"
version = "0.1.0"
description = "Sovara debug examples for CrewAI"
requires-python = ">=3.10"
dependencies = [
    "sovara",
    "crewai",
    "crewai-tools",
]

[tool.uv.sources]
sovara = { path = "../../..", editable = true }
