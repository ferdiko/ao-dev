[project]
name = "sovara-examples-anthropic"
version = "0.1.0"
description = "Sovara debug examples for Anthropic"
requires-python = ">=3.10"
dependencies = [
    "sovara",
    "anthropic",
    "e2b-code-interpreter",
]

[tool.uv.sources]
sovara = { path = "../../..", editable = true }
