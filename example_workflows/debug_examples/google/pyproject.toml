[project]
name = "sovara-examples-google"
version = "0.1.0"
description = "Sovara debug examples for Google/VertexAI"
requires-python = ">=3.10"
dependencies = [
    "sovara",
    "google-genai",
]

[tool.uv.sources]
sovara = { path = "../../..", editable = true }
